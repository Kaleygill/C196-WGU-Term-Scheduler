package com.example.myapplication;

import androidx.appcompat.app.AppCompatActivity;

public class MentorActivity extends AppCompatActivity {
/*
    private ScheduleManagementRepository scheduleManagementRepository;
    @Override
    protected void onCreate(Bundle savedInstanceState) {

        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_mentor);

        scheduleManagementRepository = new ScheduleManagementRepository((getApplication()));
        scheduleManagementRepository.getAllMentors();
        RecyclerView recyclerView = findViewById(R.id.mentors);

        final MentorAdapter adapter = new MentorAdapter(this);
        recyclerView.setAdapter(adapter);
        recyclerView.setLayoutManager(new LinearLayoutManager(this));
        adapter.setWords(scheduleManagementRepository.getAllMentors());
    }

    //Navigate to Add Mentor Screen
    public void addMentor(View view) {
        Intent intent = new Intent(MentorActivity.this, AddMentorActivity.class);
        startActivity(intent);
    }*/
}
